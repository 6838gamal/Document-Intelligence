const CONFIG = {
  // Empty string = relative URLs → same origin as the server serving this file.
  // This means the admin panel works on any hosting without any changes.
  API_BASE_URL: "",
  APP_NAME: "DocIQ — لوحة التحكم",
  TOKEN_KEY: "dociq_admin_token",
  USER_KEY: "dociq_admin_user",
};
