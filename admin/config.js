// ─────────────────────────────────────────────
//  Admin Panel Configuration
//  Change API_BASE_URL when deploying to a
//  separate static hosting (Netlify, GitHub Pages, etc.)
// ─────────────────────────────────────────────
const CONFIG = {
  API_BASE_URL: "",   // empty = same origin. Set to "https://your-backend.com" when hosted separately
  APP_NAME: "DocIQ — لوحة التحكم",
  TOKEN_KEY: "dociq_admin_token",
  USER_KEY: "dociq_admin_user",
};
